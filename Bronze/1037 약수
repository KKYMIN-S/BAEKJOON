#include <iostream>
using namespace std;

int main()
{
	int A, N, max = 0, min = 0;
	cin >> N;
	
	for (int i = 0; i < N; i++)
	{
		cin >> A;
		if (i == 0)
			min = A;
		if (A > max)
			max = A;
		if (A < min)
			min = A;
	}
	cout << max * min;
}
