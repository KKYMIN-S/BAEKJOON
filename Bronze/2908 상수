#include <iostream>
#include <string>
using namespace std;

int main() 
{
	string a, b;
	int temp1, temp2;

	cin >> a >> b;

	temp1 = a[0];
	a[0] = a[2];
	a[2] = temp1;

	temp2 = b[0];
	b[0] = b[2];
	b[2] = temp2;

	if (a > b) {
		cout << a;
	}
	else {
		cout << b;
	}
}
