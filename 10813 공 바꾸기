#include <stdio.h>
#include <stdlib.h>

int main() 
{
	int n, m, i, j;
	int temp;
	scanf("%d %d", &n, &m);

	int* arr = (int*)malloc(n * sizeof(int));

	for (int x = 0; x < n; x++)
	{
		arr[x] = x + 1;
	}
	for (int y = 0; y < m; y++)
	{
		scanf("%d %d", &i, &j);
		temp = arr[i-1];
		arr[i-1] = arr[j-1];
		arr[j-1] = temp;
	}
	for (int x = 0; x < n; x++)
	{
		printf("%d ", arr[x]);
	}
	free(arr);
}
