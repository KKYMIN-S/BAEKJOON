list_x = [] # 리스트 선언
list_y = []

for _ in range(3):
    x, y = map(int, input().split()) # map 사용
    list_x.append(x) # 리스트 내에 원소 추가
    list_y.append(y)

a = [x for x in list_x if list_x.count(x) == 1] # list_x의 원소 개수가 1개인 것
b = [y for y in list_y if list_y.count(y) == 1] # list_y의 원소 개수가 1개인 것

print(*a, *b) # []없이 출력
